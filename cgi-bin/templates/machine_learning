<!DOCTYPE html>
{% include 'templates/header' %}

	<div id="show-btn" class="cell cHead">
		<span id="top-header">
			<h4>[reader@blog.justinduch.com]$pwd</h4>
			<h4>/articles/machine-learning</h4>
		</span>
		<div>
			<span class="bar">
				<div class="bar1"></div>
				<div class="bar2"></div>
				<div class="bar3"></div>
			</span>
			<h4 style="display: inline-block;">[reader@blog.justinduch.com]$<span id="blink">_</span></h4>
		</div>
	</div>

	<div class="cell background cBody">
		<small>machine learning how to's and resources.</small>
		{% for article in stash.articles %}
		<div class="article-container">
			<h3><a class="link" href="{{article.location}}">{{article.name}}</a></h3>
			<p>{{article.blurb}}<a class="ellipsis" href="{{article.location}}">...</a></p>
			<small>Date: {{article.created}}</small>
		</div>
		{% endfor %}
	</div>

{% include 'templates/footer' %}

